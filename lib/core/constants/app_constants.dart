// Core application-wide constants
// Add new constants here as needed, keeping this file focused on simple immutable values.

class AppConstants {
  /// File name where the encrypted vault data is persisted locally.
  static const String storageFile = 'vault.json';
}
